<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="using:Clac.UI.Models"
             x:Class="Clac.UI.Views.KeyboardKeyView"
             x:DataType="models:KeyboardKey"
             Padding="0">
    
    <Button Content="{Binding Label}" x:Name="KeyButton" Click="OnKeyButtonClick" FontWeight="Bold" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
</UserControl>